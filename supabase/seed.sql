-- 시드 데이터 (Phase 1 Step 4에서 채움)
-- policies-reference.txt를 기반으로 supabase/seed_policies.sql 파일을 별도로 생성해 채우는 것을 권장.
-- 시드 적용: supabase db reset 명령으로 마이그레이션 + 시드를 한 번에 적용한다.

-- 예시 (Step 4에서 확장):
-- insert into policies (title, summary, description, provider, provider_name, category, support_type, support_amount, target_stage, source)
-- values (...);
